import { Integration } from '@sentry/types';
/** JSDoc */
export declare class Debug implements Integration {
    /**
     * @inheritDoc
     */
    name: string;
    /**
     * @inheritDoc
     */
    install(): void;
}
